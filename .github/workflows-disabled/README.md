# GitHub Workflows

Workflows inherited from the upstream project are intentionally disabled for now.

They are archived in `.github/workflows-disabled/` with a `.disabled` suffix so
they cannot run automatically after this repository is pushed to GitHub.

Before enabling any workflow, review repository names, release artifacts,
Cloudflare secrets, package names, and deployment targets for Email Transfer
Station.
